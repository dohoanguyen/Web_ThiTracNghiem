@{
    Layout = "~/Views/Shared/LayoutPage2.cshtml";
    ViewBag.Title = "Kết quả thi";
}

<div class="container text-center py-5">
    <h2 class="fw-bold text-success mb-3">Nộp bài thành công!</h2>
    <p class="fs-5">Bạn đã làm đúng <strong>@ViewBag.SoCauDung</strong> / @ViewBag.TongCau câu.</p>
    <p class="fs-4 text-primary">Điểm: <strong>@ViewBag.Diem</strong></p>

    <div class="mt-4">
        <a href="@Url.Action("DiemThi","HocVien")" class="btn btn-lg text-white px-5" style="background:#1800AC;">
            Xem bảng điểm
        </a>
    </div>
</div>

@*<div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-10">
                <div class="card shadow-lg border-0 text-center">
                    <div class="card-body py-5">
                        <div class="mb-4">
                            <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
                        </div>
                        <h2 class="fw-bold text-success mb-3">Nộp bài thành công!</h2>
                        <p class="fs-5">Bạn đã làm đúng <strong>@ViewBag.SoCauDung</strong> câu trên tổng số <strong>@ViewBag.TongCau</strong> câu.</p>
                        <p class="fs-3 fw-semibold text-primary mb-4">Điểm: <strong>@ViewBag.Diem</strong></p>

                        <a href="@Url.Action("DiemThi","HocVien")" class="btn btn-lg text-white px-5" style="background:#1800AC; border-radius:50px;">
                            Xem bảng điểm
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>*@

<style>
    .card {
        border-radius: 15px;
    }

    .btn:hover {
        background-color: #0f008a !important;
    }
</style>
